# TCP-IP-basic-Server-Client-Project
A basic project on TCP/IP Server Client using IntelliJ IDE
* Project Created using IntelliJ IDE and JDK 10
* First run Main.java from EchoServer then run Main.java from EchoClient
* You can also run Main.java(EchoClient) multiple times(parallel run) as multiple clients and check the output 
